/**
 * File: README.md
 * Module: audit
 * Purpose: Next steps for the generated audit module.
 */

# Audit module

Generated by `src/modules/module.sh`. This is a **scaffold** — implement persistence and business rules next.

## Layout

- `domain/` — entities & value objects (imports `src/core` only)
- `use-cases/` — application workflows returning `Result`
- `repositories/` — Prisma + TypeORM adapters + `AuditRepositoryProvider`
- `audit.service.ts` — orchestration + HTTP exception mapping
- `audit.controller.ts` — REST + Swagger

## Checklist

1. **Wire AppModule** — import `AuditModule` in `src/app.module.ts`.
2. **Prisma** — add a model to `prisma/schema.prisma` and implement `PrismaAuditRepository`.
3. **TypeORM** — ensure `AuditOrmEntity` is migrated when `ORM_PROVIDER=typeorm`.
4. **Auth** — uncomment guards / `Public` decorator on routes as needed.
5. **Verify** — `yarn build:app` and `yarn test`.

## ORM selection

Factory reads `orm.type`, `ORM_TYPE`, or `ORM_PROVIDER` (default `prisma`).
